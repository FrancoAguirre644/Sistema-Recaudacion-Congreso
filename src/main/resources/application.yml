  
spring:
  datasource:
    url: jdbc:mysql://localhost/sistemaRecaudacionCongreso?serverTimezone=UTC
    username: root
    password:  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false